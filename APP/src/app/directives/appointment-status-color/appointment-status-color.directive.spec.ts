import { AppointmentStatusColorDirective } from './appointment-status-color.directive';

describe('AppointmentStatusColorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppointmentStatusColorDirective();
    expect(directive).toBeTruthy();
  });
});
