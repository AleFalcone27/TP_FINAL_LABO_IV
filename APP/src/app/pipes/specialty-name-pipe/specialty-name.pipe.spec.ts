import { SpecialtyNamePipe } from './specialty-name.pipe';

describe('SpecialtyNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SpecialtyNamePipe();
    expect(pipe).toBeTruthy();
  });
});
