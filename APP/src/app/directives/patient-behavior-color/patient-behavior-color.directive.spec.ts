import { PatientBehaviorColorDirective } from './patient-behavior-color.directive';

describe('PatientBehaviorColorDirective', () => {
  it('should create an instance', () => {
    const directive = new PatientBehaviorColorDirective();
    expect(directive).toBeTruthy();
  });
});
