import { ZoomInImagesDirective } from './zoom-in-images.directive';

describe('ZoomInImagesDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomInImagesDirective();
    expect(directive).toBeTruthy();
  });
});
